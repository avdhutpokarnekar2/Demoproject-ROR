$("#target-for-change-<%= @product.id%>").val("<%= @product.quantity rescue 0 %>");
$("#target-for-total-<%= @product.id%>").html("<%= @product.quantity * @product.price rescue 0 %>");
$("#target-for-total-cart").html("<%= @cart_total[:total_price] %>");
$("#total-product-val").html("<%=@cart_total[:shipping_total]%>");
$("#finall-vall").html("<%= @cart_total[:order_total] %>");
